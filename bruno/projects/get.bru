meta {
  name: Get Project
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/json/projects/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/vnd.api+json
}

params:path {
  id: <project-uuid>
}
